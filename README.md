# Login-Signup

This a login/signup page created using MERN Stack.

To run it:- 
  1. Go to terminal type cd server, cd client, npm install, npm run start.
  2. Go to terminal type cd server, npm install, node app.js/nodemon app.js.
